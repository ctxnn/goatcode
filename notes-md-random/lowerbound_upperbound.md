# lower bound n upper bound - stl 

note: to perform binary search use upper bound and lower bound 

>NOTE
>values: [10, 20, 30, 40, 50]
x = 30
lower_bound(x) → 30
upper_bound(x) → 40


want smallest ≥ x -> use lower_bound(x)
want largest ≤ x -> use upper_bound(x)